import {View, Text} from 'react-native';

const DynamicScreen = () => {
  return (
    <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
      <Text className="text-red-500">Dynamic</Text>
    </View>
  );
};
export default DynamicScreen;
